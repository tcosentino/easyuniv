// Filename: entry.js
// Purpose : entry.js is the collection structure for the app object.

define([
  'underscore',
  'backbone'
], function(_, Backbone) {
	var EntryModel = Backbone.Model.extend({
		//nothing yet
	});
	return EntryModel;
});